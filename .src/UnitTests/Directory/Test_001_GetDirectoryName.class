' Gambas class file

Static Public Sub Main()

  Dim InputValue As String = "/tmp/.testing"
  Dim Expected As String = "/tmp"
  Dim Actual As String = Directory.GetDirectoryName(InputValue)

  Assert.Section = "Directory"
  Assert.Test = "Test_001_GetDirectoryName"
  Assert.IsExpected(Actual, Expected, "Directory Name Incorrect")

End
