' Gambas class file

Static Public Sub Main()

  Dim Store As New Credentials

  Assert.Section = "Credentials"
  Assert.Test = Class(Me).Name

  Assert.IsTrue(Store.List.Count > 0, "No Credentials Found in Settings")

End
