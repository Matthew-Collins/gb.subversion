' Gambas class file

Static Public Sub Main()

  Dim List As String
  Dim Actual As Boolean = Global.Run.Execute(["ls"], Null, True, True, ByRef List)

  Assert.Section = "Executor"
  Assert.Test = "Test_001_DisplayReadAndErrors"
  Assert.IsTrue(Actual, "Errors Detected")
  Assert.IsNotExpected(Split(List, "\n").Count, 0, "Return List Is Empty")

End
